import { ChevronDown } from "lucide-react";
import type { LeagueStanding } from "@/lib/league-standings";

export default function MobileStandings({ standings, yourTeam }: { standings: LeagueStanding[]; yourTeam?: string }) {
  return <div className="space-y-3 md:hidden">{standings.map((standing) => <details className={`group rounded-xl border bg-card shadow-sm ${standing.team_name === yourTeam ? "border-primary ring-1 ring-primary/20" : ""}`} key={standing.team_name}><summary className="flex cursor-pointer list-none items-center gap-3 p-4"><span className="flex size-9 shrink-0 items-center justify-center rounded-full bg-muted text-sm font-bold">#{standing.rank}</span><div className="min-w-0 flex-1"><p className="truncate font-semibold">{standing.team_name}</p><p className="mt-0.5 text-sm text-muted-foreground">{standing.win_count}–{standing.lost_count}–{standing.push_count} · {standing.win_percentage}% wins</p></div><div className="text-right"><p className="font-bold tabular-nums">{standing.result_points}</p><p className="text-xs text-muted-foreground">points</p></div><ChevronDown className="size-4 text-muted-foreground transition-transform group-open:rotate-180" /></summary><div className="grid grid-cols-3 gap-3 border-t bg-muted/20 p-4 text-center text-sm"><div><p className="text-muted-foreground">Picks</p><p className="mt-1 font-semibold">{standing.pick_count}</p></div><div><p className="text-muted-foreground">Favorites</p><p className="mt-1 font-semibold">{standing.fav_count}</p></div><div><p className="text-muted-foreground">Underdogs</p><p className="mt-1 font-semibold">{standing.und_count}</p></div></div></details>)}</div>;
}
